<mjml lang="de">
    <mj-head>
        <mj-include path="../partials/head.mjml"/>

        <mj-preview>Mitteilung über das Ende einer Aufgabenzuweisung</mj-preview>
    </mj-head>
    <mj-body>
        <mj-include path="../partials/header.mjml"/>

        <mj-section>
            <mj-column>
                <mj-text>
                    <p>
                        Hallo,
                        <br/>
                        Die Ihnen zugewiesene Aufgabe wurde einer anderen Person zugewiesen.
                        <br/>
                        <br/>
                        Prozess: <b th:text="${process.internalTitle}">Beispielprozess</b> - Version
                        <b th:text="${processInstanceTask.processVersion}">1</b>
                        <br/>
                        Aufgabe: <b th:text="${taskName}">Beispielaufgabe</b>
                        <span th:if="${fileNumbersDisplay != null}">
                            <br/>
                            Aktenzeichen: <b th:text="${fileNumbersDisplay}">AZ-123</b>
                        </span>
                        <br/>
                        Geändert durch: <b th:text="${changeSource}">System</b>
                    </p>
                </mj-text>
            </mj-column>
        </mj-section>

        <mj-include path="../partials/default-mail-signature.mjml"/>
        <mj-include path="../partials/auto-generate-hint.mjml"/>
    </mj-body>
</mjml>
