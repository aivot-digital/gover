package de.aivot.prosuna.backend.process.enums;

/** Identifies whether a node configuration is validated while modeling or after runtime values were resolved. */
public enum ProcessNodeConfigurationValidationPhase {
    Authoring,
    Runtime
}
