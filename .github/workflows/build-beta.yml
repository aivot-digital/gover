name: Build Beta

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - edited
    branches:
      - "main"

jobs:
  prepareBuildVars:
    name: Prepare
    # Run if the pr is not a draft and the trigger it's a normal trigger (open/sync/reopen) OR if it's an 'edited' event where the base branch changed
    if: github.event.pull_request.draft == false && (github.event.action != 'edited' || github.event.changes.base)
    runs-on: ubuntu-24.04
    outputs:
      build_date: ${{ steps.build_date.outputs.value }}
      build_version: ${{ steps.build_version.outputs.value }}
      build_number: ${{ github.run_number }}
    steps:
      - name: Get current date
        id: build_date
        run: echo "value=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"
      - name: Get the current build version based on the source branch
        id: build_version
        env:
          SOURCE_BRANCH: ${{ github.event.pull_request.head.ref }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          build_version="${SOURCE_BRANCH##*/}-beta-${PR_NUMBER}"
          semver_pattern='^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-((0|[1-9][0-9]*|[0-9A-Za-z-]*[A-Za-z-][0-9A-Za-z]*)(\.(0|[1-9][0-9]*|[0-9A-Za-z-]*[A-Za-z-][0-9A-Za-z]*))*))?(\+([0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*))?$'
          if [[ "$build_version" =~ $semver_pattern ]]; then
            echo "value=$build_version" >> "$GITHUB_OUTPUT"
          else
            echo "::notice title=Invalid build version::Computed build version '$build_version' is not valid SemVer. Falling back to 0.0.0."
            echo "value=0.0.0" >> "$GITHUB_OUTPUT"
          fi

  testBackend:
    name: Test Backend
    # Run if the pr is not a draft and the trigger it's a normal trigger (open/sync/reopen) OR if it's an 'edited' event where the base branch changed
    if: github.event.pull_request.draft == false && (github.event.action != 'edited' || github.event.changes.base)
    needs:
      - prepareBuildVars
    uses: ./.github/workflows/test-backend.yml
    permissions:
      contents: read
    with:
      build_version: ${{ needs.prepareBuildVars.outputs.build_version }}

  generateSbom:
    name: SBOM
    # Run if the pr is not a draft and the trigger it's a normal trigger (open/sync/reopen) OR if it's an 'edited' event where the base branch changed
    if: github.event.pull_request.draft == false && (github.event.action != 'edited' || github.event.changes.base)
    needs:
      - prepareBuildVars
    uses: ./.github/workflows/generate-sbom.yml
    permissions:
      contents: read
      issues: write
    with:
      build_version: ${{ needs.prepareBuildVars.outputs.build_version }}
      build_date: ${{ needs.prepareBuildVars.outputs.build_date }}
    secrets: inherit

  genrateLibrary:
    name: Java Library
    # Run if the pr is not a draft and the trigger it's a normal trigger (open/sync/reopen) OR if it's an 'edited' event where the base branch changed
    if: github.event.pull_request.draft == false && (github.event.action != 'edited' || github.event.changes.base)
    needs:
      - prepareBuildVars
      - testBackend
      - generateSbom
    uses: ./.github/workflows/generate-library.yml
    permissions:
      contents: read
      packages: write
    with:
      build_version: ${{ needs.prepareBuildVars.outputs.build_version }}
    secrets: inherit

  generateImage:
    name: Container Image
    # Run if the pr is not a draft and the trigger it's a normal trigger (open/sync/reopen) OR if it's an 'edited' event where the base branch changed
    if: github.event.pull_request.draft == false && (github.event.action != 'edited' || github.event.changes.base)
    needs:
      - prepareBuildVars
      - testBackend
      - generateSbom
    uses: ./.github/workflows/generate-image.yml
    permissions:
      contents: read
      packages: write
    with:
      build_version: ${{ needs.prepareBuildVars.outputs.build_version }}
      build_date: ${{ needs.prepareBuildVars.outputs.build_date }}
      build_number: ${{ needs.prepareBuildVars.outputs.build_number }}
      image_name: ${{ github.repository_owner }}/gover-next
      is_release: false
    secrets: inherit
